# 水滴鱼 2.7.3 · 更新退出修复与快捷传话

- 修复串门状态下退出可能一直等待的问题，退出前收尾加入独立超时。
- 更新重启改为等待旧进程退出后再启动新版；防止连续点击启动多次安装。
- 菜单栏改为**透明背景、原色粉色水滴鱼**，替换鱼 emoji。
- 迷你传话框直接显示互动与串门小图标，取消更多菜单，保持 240×132 pt 默认内容区。
- 聊天记录底部恢复发送区，按好友分别保留草稿；回车发送，Command＋回车换行。

原生自检 97/97，消息及设置外观代码检查 8/8 通过；未进行真实好友消息、全屏或视觉验收。

## 安装

- Apple Silicon：`BlobfishNative-2.7.3-macOS-arm64.zip`
- Intel：`BlobfishNative-2.7.3-macOS-x64.zip`
- 自动更新：`blobfish-native-latest.json`

继续使用 ad-hoc 签名，不含 Apple Developer ID，不做公证。从旧版升级时若旧进程仍卡在退出，请退出旧进程后重新打开新版；替换磁盘安装包不会改变仍驻留的旧代码。
